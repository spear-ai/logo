name: auto-release
on:
  push:
    branches: main
jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: google-github-actions/release-please-action@v3.0.1
        with:
          command: manifest
